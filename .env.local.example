# MongoDB Connection URI
# Replace with your MongoDB Atlas URI or local MongoDB connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

# NextAuth.js Secret - Generate a secure random string
# You can use: `openssl rand -base64 32` or https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your_nextauth_secret_here

# For absolute URLs (e.g. in emails, auth callbacks)
NEXTAUTH_URL=http://localhost:3000 